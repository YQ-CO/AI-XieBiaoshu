# 开发计划完成度简表（2026-03-01）

## 1) 阶段完成概览

- 第 1 周（测试框架 + 最小 CI）：**已完成**
- 第 2 周（关键集成测试 + 关键链路门禁）：**已完成（并扩展）**
- 第 3 周（性能基线 + 双节奏执行）：**已完成**
- 第 4 周（安全扫描矩阵 + 分层治理）：**已完成（容器镜像扫描待有镜像后启用）**

## 2) 已完成能力清单

### 测试与质量门禁
- 后端：`lint`、`typecheck`、`unit`、`integration`、`build` 已接入 CI。
- 前端：`typecheck`、`unit`、`build` 已接入 CI。
- 前端 E2E：Playwright smoke 已接入，含正向与权限负向场景。

### 集成与权限回归
- 已覆盖登录、充值/订单、发票审核、异步任务、CSV 导出、Word 导出。
- 已覆盖权限负向：普通用户访问 admin 链路被 403/重定向拦截。

### 性能
- `CI` 主工作流含 k6 基线任务（主干自动，支持手动参数）。
- `Performance Schedule` 已落地 nightly + weekly 双节奏，支持手动档位选择。
- 性能 artifact 保留 90 天。

### 安全
- 依赖漏洞：`npm audit --audit-level=high` 阻断。
- 密钥泄露：`gitleaks` 阻断。
- 静态扫描：`semgrep` 分层治理（observe + blocking）。
- 文件系统扫描：`trivy fs`（HIGH/CRITICAL 阻断）。

### 文档与执行指引
- 已有发布前清单文档。
- 已有手动触发性能任务指引。
- 已有本地 semgrep 分层命令与说明。

## 3) 仍待完成（严格按最初目标口径）

- 前端 E2E 目前为 smoke 基线，尚未扩展到更完整业务路径矩阵。
- 容器镜像扫描（Trivy image）需在引入 Dockerfile/镜像产物后启用；当前为 fs 扫描。
- 安全规则分层需按 2~4 周节奏复盘，逐步把低噪音规则升为阻断。

## 4) 下一步建议（最小增量）

1. 运行一次手动性能验收（CI + Performance Schedule），确认产物与阈值稳定。
2. 连续两周观察 semgrep/trivy 噪音水平，确定下一批提升为阻断的规则。
3. 选择 2 条高价值 E2E 业务路径补充到 smoke 集中（避免过度扩张）。
